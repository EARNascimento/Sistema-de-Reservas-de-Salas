'use client';
import {useEffect,useState} from 'react';
import type {LucideIcon} from 'lucide-react';
import {CalendarDays,ChevronRight,Clock3,DoorOpen,GraduationCap,LayoutDashboard,LogOut,Menu, Moon, Plus, Search, Settings2, ShieldCheck, Sun, Users, X, Check, Ban, Building2, Sparkles} from 'lucide-react';
import {rooms as initialRooms,reservations as initialReservations,users} from '@/data/mock'; import {loadState,saveState,clearSession} from '@/lib/storage'; import {Reservation,Room,RoomStatus,User} from '@/types';
const fmt=(d:string)=>new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'short'}).format(new Date(d+'T12:00:00')).replace('.','');
const today=new Date(); today.setDate(today.getDate()+2); const defaultDate=today.toISOString().slice(0,10);
function Status({status}:{status:RoomStatus}){const s={available:['Livre','bg-emerald-50 text-emerald-700'],occupied:['Ocupada','bg-amber-50 text-amber-700'],blocked:['Bloqueada','bg-rose-50 text-rose-700']}[status];return <span className={'inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-bold '+s[1]}><i className="h-1.5 w-1.5 rounded-full bg-current"/>{s[0]}</span>}
function Login({onLogin}:{onLogin:(u:User)=>void}){const [mode,setMode]=useState<'student'|'admin'>('student');const [email,setEmail]=useState(mode==='student'?'aluno@instituicao.edu.br':'admin@instituicao.edu.br');const [pass,setPass]=useState(mode==='student'?'123456':'admin123');const [error,setError]=useState(''); useEffect(()=>{setEmail(mode==='student'?'aluno@instituicao.edu.br':'admin@instituicao.edu.br');setPass(mode==='student'?'123456':'admin123')},[mode]);return <main className="flex min-h-screen items-center justify-center overflow-hidden bg-slate-50 p-4"><div className="absolute left-0 top-0 h-72 w-72 rounded-full bg-violet-200/40 blur-3xl"/><div className="absolute bottom-0 right-0 h-96 w-96 rounded-full bg-blue-200/30 blur-3xl"/><section className="glass shadow-soft relative grid w-full max-w-5xl overflow-hidden rounded-[28px] md:grid-cols-2"><div className="gradient hidden min-h-[620px] flex-col justify-between p-10 text-white md:flex"><div><div className="mb-16 flex items-center gap-2 text-lg font-bold"><div className="rounded-xl bg-white/20 p-2"><DoorOpen size={20}/></div>Nexo Salas</div><p className="mb-5 text-sm font-semibold uppercase tracking-[.22em] text-white/70">Seu espaço, no seu ritmo</p><h1 className="max-w-sm text-5xl font-bold leading-[1.08]">Encontre o lugar certo para criar.</h1><p className="mt-6 max-w-sm text-base leading-7 text-white/75">Reserve ambientes acadêmicos em poucos cliques e transforme sua próxima ideia em realidade.</p></div><div className="flex items-center gap-3 text-sm text-white/75"><Sparkles size={17}/> Experiência simples para uma rotina mais leve</div></div><div className="p-7 sm:p-12"><div className="mb-9 flex items-center gap-2 text-xl font-bold md:hidden"><div className="gradient rounded-xl p-2 text-white"><DoorOpen size={18}/></div>Nexo Salas</div><p className="text-sm font-semibold text-violet-600">Bem-vindo de volta</p><h2 className="mt-2 text-3xl font-bold tracking-tight">Acesse sua conta</h2><p className="mt-2 text-sm text-slate-500">Gerencie seu espaço acadêmico com facilidade.</p><div className="mt-8 grid grid-cols-2 rounded-xl bg-slate-100 p-1"><button onClick={()=>setMode('student')} className={'rounded-lg py-2.5 text-sm font-semibold transition '+(mode==='student'?'bg-white text-violet-700 shadow-sm':'text-slate-500')}>Aluno</button><button onClick={()=>setMode('admin')} className={'rounded-lg py-2.5 text-sm font-semibold transition '+(mode==='admin'?'bg-white text-violet-700 shadow-sm':'text-slate-500')}>Administrador</button></div><form className="mt-7 space-y-5" onSubmit={e=>{e.preventDefault();const u=users.find(x=>x.email===email&&((mode==='student'&&pass==='123456')||(mode==='admin'&&pass==='admin123')));if(u)onLogin(u);else setError('E-mail ou senha inválidos.');}}><label className="block text-sm font-semibold">E-mail institucional<input aria-label="E-mail" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3.5 text-sm outline-none transition focus:border-violet-500 focus:ring-4 focus:ring-violet-100" type="email"/></label><label className="block text-sm font-semibold">Senha<input aria-label="Senha" value={pass} onChange={e=>setPass(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3.5 text-sm outline-none transition focus:border-violet-500 focus:ring-4 focus:ring-violet-100" type="password"/></label>{error&&<p className="text-sm font-medium text-rose-600">{error}</p>}<button className="gradient flex w-full items-center justify-center gap-2 rounded-xl py-3.5 font-bold text-white shadow-lg shadow-violet-200 transition hover:-translate-y-0.5 active:scale-[.98]">Entrar <ChevronRight size={18}/></button></form><p className="mt-7 text-center text-xs text-slate-400">Acesso seguro • Ambiente institucional</p></div></section></main>}
function Shell({user,onLogout,children,active,setActive,dark,setDark}:{user:User;onLogout:()=>void;children:React.ReactNode;active:string;setActive:(x:string)=>void;dark:boolean;setDark:(x:boolean)=>void}){const admin=user.role==='admin';const nav:[string,string,LucideIcon][]=admin?[['overview','Visão geral',LayoutDashboard],['reservations','Reservas',CalendarDays],['rooms','Salas',DoorOpen]]:[['catalog','Catálogo de salas',DoorOpen],['mine','Minhas reservas',CalendarDays]];return <div className="flex min-h-screen bg-[var(--bg)]"><aside className="fixed inset-y-0 left-0 z-20 hidden w-64 flex-col border-r border-slate-100 bg-white px-5 py-7 lg:flex dark:bg-slate-900"><div className="mb-12 flex items-center gap-2 px-2 text-lg font-bold"><div className="gradient rounded-xl p-2 text-white"><DoorOpen size={18}/></div>Nexo <span className="text-violet-600">Salas</span></div><div className="mb-4 px-2 text-[10px] font-bold uppercase tracking-[.2em] text-slate-400">Workspace</div><nav className="space-y-1">{nav.map(([id,label,Icon])=><button key={id} onClick={()=>setActive(id as string)} className={'flex w-full items-center gap-3 rounded-xl px-3 py-3 text-left text-sm font-semibold transition '+(active===id?'bg-violet-50 text-violet-700':'text-slate-500 hover:bg-slate-50')}><Icon size={18}/>{label}</button>)}</nav><div className="mt-auto space-y-2"><button onClick={()=>setDark(!dark)} className="flex w-full items-center gap-3 rounded-xl px-3 py-3 text-sm font-semibold text-slate-500 hover:bg-slate-50">{dark?<Sun size={18}/>:<Moon size={18}/>} {dark?'Modo claro':'Modo escuro'}</button><div className="flex items-center gap-3 border-t border-slate-100 px-2 pt-5"><div className="gradient flex h-9 w-9 items-center justify-center rounded-full text-xs font-bold text-white">{user.avatar}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{user.name}</p><p className="text-xs capitalize text-slate-400">{admin?'Administrador':'Aluno'}</p></div><button onClick={onLogout} title="Sair"><LogOut size={17} className="text-slate-400"/></button></div></div></aside><div className="w-full lg:pl-64"><header className="sticky top-0 z-10 flex h-[74px] items-center justify-between border-b border-slate-100 bg-white/85 px-5 backdrop-blur-md sm:px-8 dark:bg-slate-900/85"><div className="flex items-center gap-3 lg:hidden"><div className="gradient rounded-lg p-1.5 text-white"><DoorOpen size={17}/></div><b>Nexo Salas</b></div><div className="hidden text-sm text-slate-500 lg:block">{admin?'Painel administrativo':'Central de reservas'} <span className="mx-2">/</span> <b className="text-slate-900 dark:text-white">{nav.find(x=>x[0]===active)?.[1]}</b></div><div className="flex items-center gap-3"><button onClick={()=>setDark(!dark)} className="rounded-xl p-2.5 text-slate-500 hover:bg-slate-100 lg:hidden">{dark?<Sun size={18}/>:<Moon size={18}/>}</button><div className="gradient flex h-9 w-9 items-center justify-center rounded-full text-xs font-bold text-white lg:hidden">{user.avatar}</div></div></header><main className="mx-auto max-w-[1400px] p-5 sm:p-8">{children}</main></div></div>}
function RoomCard({room,onReserve}:{room:Room;onReserve:(r:Room)=>void}){return <article className="rise shadow-soft group overflow-hidden rounded-2xl border border-slate-100 bg-white transition hover:-translate-y-1 hover:shadow-xl"><div className={'relative h-28 overflow-hidden bg-gradient-to-br '+({'violet':'from-violet-500 to-indigo-600','blue':'from-blue-500 to-cyan-500','cyan':'from-cyan-500 to-teal-500','orange':'from-orange-400 to-rose-500','emerald':'from-emerald-400 to-teal-600','pink':'from-pink-400 to-violet-500','indigo':'from-indigo-500 to-blue-700','amber':'from-amber-400 to-orange-500','rose':'from-rose-500 to-red-600'}[room.accent]||'from-violet-500 to-indigo-600')}><div className="absolute -right-5 -top-12 h-40 w-40 rounded-full border-[22px] border-white/10"/><div className="absolute bottom-4 left-5 text-white"><p className="text-xs font-medium text-white/75">{room.building} • {room.floor}</p><h3 className="mt-1 text-xl font-bold">{room.name}</h3></div><div className="absolute right-4 top-4"><Status status={room.status}/></div></div><div className="p-5"><div className="flex items-center justify-between text-sm text-slate-500"><span className="flex items-center gap-1.5"><Users size={16}/> Até <b className="text-slate-800">{room.capacity} pessoas</b></span><span className="flex items-center gap-1.5"><Clock3 size={15}/> 07:00–22:00</span></div><div className="mt-4 flex flex-wrap gap-2">{room.features.map(f=><span key={f} className="rounded-md bg-slate-50 px-2 py-1 text-[11px] font-semibold text-slate-500">{f}</span>)}</div><button disabled={room.status!=='available'} onClick={()=>onReserve(room)} className={'mt-5 flex w-full items-center justify-center gap-2 rounded-xl py-3 text-sm font-bold transition '+(room.status==='available'?'bg-violet-50 text-violet-700 hover:bg-violet-600 hover:text-white':'cursor-not-allowed bg-slate-100 text-slate-400')}>{room.status==='available'?<><Plus size={17}/> Reservar sala</>:room.status==='blocked'?'Indisponível para manutenção':'Em uso agora'}</button></div></article>}
function Modal({room,user,onClose,onConfirm}:{room:Room;user:User;onClose:()=>void;onConfirm:(r:Reservation)=>void}){const [date,setDate]=useState(defaultDate),[start,setStart]=useState('09:00'),[end,setEnd]=useState('10:00'),[reason,setReason]=useState('');return <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-950/45 p-4 backdrop-blur-sm"><div className="w-full max-w-lg rounded-3xl bg-white p-6 shadow-2xl sm:p-8"><div className="flex items-start justify-between"><div><span className="text-xs font-bold uppercase tracking-wider text-violet-600">Nova reserva</span><h2 className="mt-1 text-2xl font-bold">{room.name}</h2><p className="mt-1 text-sm text-slate-500">{room.building} • capacidade para {room.capacity} pessoas</p></div><button onClick={onClose} className="rounded-full bg-slate-100 p-2 text-slate-500"><X size={18}/></button></div><div className="mt-7 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold sm:col-span-2">Data<input type="date" min={new Date().toISOString().slice(0,10)} value={date} onChange={e=>setDate(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-3 outline-none focus:border-violet-500"/></label><label className="text-sm font-semibold">Início<select value={start} onChange={e=>setStart(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-3 outline-none focus:border-violet-500">{['07:00','08:00','09:00','10:00','11:00','13:00','14:00','15:00','16:00','17:00','18:00','19:00','20:00'].map(x=><option key={x}>{x}</option>)}</select></label><label className="text-sm font-semibold">Fim<select value={end} onChange={e=>setEnd(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-3 outline-none focus:border-violet-500">{['08:00','09:00','10:00','11:00','12:00','14:00','15:00','16:00','17:00','18:00','19:00','20:00','21:00','22:00'].map(x=><option key={x}>{x}</option>)}</select></label><label className="text-sm font-semibold sm:col-span-2">Motivo<input placeholder="Ex.: Estudo em grupo, reunião..." value={reason} onChange={e=>setReason(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 px-3 py-3 outline-none focus:border-violet-500"/></label></div><div className="mt-7 flex gap-3"><button onClick={onClose} className="flex-1 rounded-xl border border-slate-200 py-3 font-bold text-slate-500">Cancelar</button><button disabled={!reason||start>=end} onClick={()=>onConfirm({id:'res'+Date.now(),roomId:room.id,userId:user.id,date,start,end,reason,status:'confirmed'})} className="gradient flex flex-1 items-center justify-center gap-2 rounded-xl py-3 font-bold text-white disabled:cursor-not-allowed disabled:opacity-50"><Check size={17}/> Confirmar</button></div></div></div>}
function Toast({text,onClose}:{text:string;onClose:()=>void}){useEffect(()=>{const t=setTimeout(onClose,3500);return()=>clearTimeout(t)},[onClose]);return <div className="fixed bottom-5 right-5 z-[60] flex items-center gap-3 rounded-2xl bg-slate-900 px-4 py-3 text-sm font-semibold text-white shadow-xl"><span className="rounded-full bg-emerald-400 p-1"><Check size={13}/></span>{text}<button onClick={onClose}><X size={15}/></button></div>}
export default function App(){const [user,setUser]=useState<User|null>(null),[rooms,setRooms]=useState<Room[]>(initialRooms),[reservations,setReservations]=useState<Reservation[]>(initialReservations),[active,setActive]=useState('catalog'),[dark,setDark]=useState(false),[modal,setModal]=useState<Room|null>(null),[toast,setToast]=useState(''),[capacity,setCapacity]=useState('all'),[query,setQuery]=useState('');useEffect(()=>{const s=loadState();setUser(s.user);setRooms(s.rooms);setReservations(s.reservations);if(s.user?.role==='admin')setActive('overview')},[]);useEffect(()=>{document.documentElement.classList.toggle('dark',dark)},[dark]);const persist=(u:User|null,r:Reservation[],rm:Room[])=>{setUser(u);setReservations(r);setRooms(rm);saveState(u,r,rm)};const login=(u:User)=>{persist(u,reservations,rooms);setActive(u.role==='admin'?'overview':'catalog')};const logout=()=>{clearSession();setUser(null)};if(!user)return <Login onLogin={login}/>;const my=reservations.filter(r=>r.userId===user.id&&r.status==='confirmed');const activeRes=reservations.filter(r=>r.status==='confirmed');const filtered=rooms.filter(r=>(capacity==='all'||r.capacity>=Number(capacity))&&r.name.toLowerCase().includes(query.toLowerCase()));const cancel=(id:string)=>{if(!confirm('Cancelar esta reserva?'))return;const next=reservations.map(r=>r.id===id?{...r,status:'cancelled' as const}:r);persist(user,next,rooms);setToast('Reserva cancelada com sucesso')};const toggleBlock=(id:string)=>{const rm=rooms.map(r=>r.id===id?{...r,status:r.status==='blocked'?'available' as const:'blocked' as const}:r);persist(user,reservations,rm);setToast('Status da sala atualizado')};return <Shell {...{user,onLogout:logout,active,setActive,dark,setDark}}>{active==='catalog'&&<><div className="rise mb-8 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="mb-2 flex items-center gap-2 text-sm font-bold text-violet-600"><Sparkles size={15}/> Olá, {user.name.split(' ')[0]}</p><h1 className="text-3xl font-bold tracking-tight sm:text-4xl">Encontre seu próximo espaço</h1><p className="mt-2 text-slate-500">Salas prontas para suas melhores ideias.</p></div><button onClick={()=>setActive('mine')} className="flex w-fit items-center gap-2 rounded-xl bg-white px-4 py-3 text-sm font-bold text-slate-600 shadow-sm ring-1 ring-slate-100">Ver minhas reservas <ChevronRight size={16}/></button></div><div className="mb-6 flex flex-col gap-3 rounded-2xl border border-slate-100 bg-white p-3 shadow-sm sm:flex-row"><div className="flex flex-1 items-center gap-3 rounded-xl bg-slate-50 px-4"><Search size={18} className="text-slate-400"/><input aria-label="Buscar sala" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar por nome da sala..." className="w-full bg-transparent py-3 text-sm outline-none"/></div><select value={capacity} onChange={e=>setCapacity(e.target.value)} className="rounded-xl bg-slate-50 px-4 py-3 text-sm font-semibold text-slate-600 outline-none"><option value="all">Qualquer capacidade</option><option value="8">8+ pessoas</option><option value="16">16+ pessoas</option><option value="30">30+ pessoas</option></select></div><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{filtered.map(r=><RoomCard key={r.id} room={r} onReserve={setModal}/>)}</div>{!filtered.length&&<div className="rounded-2xl bg-white p-12 text-center"><Search className="mx-auto text-slate-300" size={38}/><h3 className="mt-3 font-bold">Nenhuma sala encontrada</h3><p className="mt-1 text-sm text-slate-500">Tente ajustar seus filtros.</p></div>}</>}{active==='mine'&&<><PageTitle title="Minhas reservas" subtitle="Acompanhe seus próximos compromissos e organize sua agenda."/><div className="grid gap-4">{my.map(r=>{const room=rooms.find(x=>x.id===r.roomId)!;return <div key={r.id} className="shadow-soft flex flex-col gap-4 rounded-2xl bg-white p-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-4"><div className="gradient rounded-2xl p-3 text-white"><CalendarDays size={22}/></div><div><h3 className="font-bold">{room.name}</h3><p className="mt-1 text-sm text-slate-500">{fmt(r.date)} • {r.start}–{r.end} • {r.reason}</p></div></div><button onClick={()=>cancel(r.id)} className="flex items-center justify-center gap-2 rounded-xl border border-rose-100 px-4 py-2.5 text-sm font-bold text-rose-600 hover:bg-rose-50"><X size={16}/> Cancelar</button></div>})}{!my.length&&<Empty text="Você ainda não possui reservas." action="Explorar salas" onClick={()=>setActive('catalog')}/>}</div></>}{active==='overview'&&<AdminOverview rooms={rooms} reservations={activeRes} users={users} setActive={setActive}/>} {active==='reservations'&&<AdminReservations rooms={rooms} reservations={activeRes} users={users} cancel={cancel}/>} {active==='rooms'&&<AdminRooms rooms={rooms} toggle={toggleBlock}/>} {modal&&<Modal room={modal} user={user} onClose={()=>setModal(null)} onConfirm={r=>{const next=[...reservations,r];persist(user,next,rooms);setModal(null);setActive('mine');setToast('Reserva confirmada com sucesso')}}/>}{toast&&<Toast text={toast} onClose={()=>setToast('')}/>}</Shell>}
function PageTitle({title,subtitle}:{title:string;subtitle:string}){return <div className="rise mb-8"><h1 className="text-3xl font-bold tracking-tight">{title}</h1><p className="mt-2 text-slate-500">{subtitle}</p></div>};function Empty({text,action,onClick}:{text:string;action:string;onClick:()=>void}){return <div className="rounded-2xl bg-white p-12 text-center"><CalendarDays className="mx-auto text-slate-300" size={40}/><p className="mt-3 text-sm text-slate-500">{text}</p><button onClick={onClick} className="mt-4 rounded-xl bg-violet-600 px-4 py-2 text-sm font-bold text-white">{action}</button></div>};
function AdminOverview({rooms,reservations,users,setActive}:{rooms:Room[];reservations:Reservation[];users:User[];setActive:(s:string)=>void}){const stats:[string,number,typeof Building2,string][]=[['Salas totais',rooms.length,Building2,'text-violet-600 bg-violet-50'],['Disponíveis',rooms.filter(r=>r.status==='available').length,Check,'text-emerald-600 bg-emerald-50'],['Em uso',rooms.filter(r=>r.status==='occupied').length,Clock3,'text-amber-600 bg-amber-50'],['Bloqueadas',rooms.filter(r=>r.status==='blocked').length,Ban,'text-rose-600 bg-rose-50']];return <><PageTitle title="Visão geral" subtitle="Uma visão clara do que está acontecendo no seu campus hoje."/><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(([t,n,I,c])=><div key={t as string} className="shadow-soft rounded-2xl bg-white p-5"><div className={'mb-5 flex h-10 w-10 items-center justify-center rounded-xl '+c as string}><I size={19}/></div><p className="text-sm text-slate-500">{t as string}</p><p className="mt-1 text-3xl font-bold">{n as number}</p></div>)}</div><div className="mt-6 grid gap-6 xl:grid-cols-[1.3fr_.7fr]"><div className="shadow-soft rounded-2xl bg-white p-5"><div className="mb-5 flex items-center justify-between"><h2 className="font-bold">Reservas recentes</h2><button onClick={()=>setActive('reservations')} className="text-sm font-bold text-violet-600">Ver todas</button></div><div className="space-y-1">{reservations.slice(0,5).map(r=><Row key={r.id} r={r} rooms={rooms} users={users}/>)}</div></div><div className="gradient shadow-soft rounded-2xl p-6 text-white"><ShieldCheck size={25}/><h2 className="mt-12 text-2xl font-bold">Tudo sob controle</h2><p className="mt-2 text-sm leading-6 text-white/75">Gerencie a disponibilidade dos ambientes e mantenha a operação fluida.</p><button onClick={()=>setActive('rooms')} className="mt-6 rounded-xl bg-white/15 px-4 py-2.5 text-sm font-bold backdrop-blur hover:bg-white/25">Gerenciar salas <ChevronRight className="inline" size={15}/></button></div></div></>}
function Row({r,rooms,users}:{r:Reservation;rooms:Room[];users:User[]}){const room=rooms.find(x=>x.id===r.roomId),u=users.find(x=>x.id===r.userId);return <div className="flex items-center gap-3 rounded-xl p-3 hover:bg-slate-50"><div className="gradient flex h-9 w-9 items-center justify-center rounded-full text-[10px] font-bold text-white">{u?.avatar}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold">{u?.name}</p><p className="text-xs text-slate-500">{room?.name} • {r.start}–{r.end}</p></div><span className="hidden text-xs font-semibold text-slate-400 sm:block">{fmt(r.date)}</span></div>}
function AdminReservations({rooms,reservations,users,cancel}:{rooms:Room[];reservations:Reservation[];users:User[];cancel:(id:string)=>void}){return <><PageTitle title="Todas as reservas" subtitle="Acompanhe e administre os agendamentos do campus."/><div className="shadow-soft overflow-hidden rounded-2xl bg-white"><div className="hidden grid-cols-[1.2fr_1fr_1fr_1fr_auto] gap-4 border-b border-slate-100 px-6 py-4 text-xs font-bold uppercase tracking-wider text-slate-400 md:grid"><span>Aluno</span><span>Sala</span><span>Data</span><span>Horário</span><span/></div>{reservations.map(r=>{const room=rooms.find(x=>x.id===r.roomId),u=users.find(x=>x.id===r.userId);return <div key={r.id} className="grid gap-2 border-b border-slate-100 px-5 py-4 last:border-0 md:grid-cols-[1.2fr_1fr_1fr_1fr_auto] md:items-center md:gap-4 md:px-6"><div className="flex items-center gap-3"><div className="gradient flex h-8 w-8 items-center justify-center rounded-full text-[10px] font-bold text-white">{u?.avatar}</div><div><p className="text-sm font-bold">{u?.name}</p><p className="text-xs text-slate-400">{u?.email}</p></div></div><span className="text-sm font-semibold">{room?.name}</span><span className="text-sm text-slate-500">{fmt(r.date)}</span><span className="text-sm text-slate-500">{r.start}–{r.end}</span><button onClick={()=>cancel(r.id)} className="w-fit rounded-lg p-2 text-slate-400 hover:bg-rose-50 hover:text-rose-600" title="Cancelar reserva"><X size={17}/></button></div>})}</div></>}
function AdminRooms({rooms,toggle}:{rooms:Room[];toggle:(id:string)=>void}){return <><PageTitle title="Gestão de salas" subtitle="Bloqueie ambientes para manutenção ou libere-os novamente."/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{rooms.map(r=><div key={r.id} className="shadow-soft rounded-2xl bg-white p-5"><div className="flex items-start justify-between"><div className="flex items-center gap-3"><div className="rounded-xl bg-violet-50 p-3 text-violet-600"><DoorOpen size={20}/></div><div><h3 className="font-bold">{r.name}</h3><p className="text-xs text-slate-500">{r.building} • {r.capacity} lugares</p></div></div><Status status={r.status}/></div><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4"><span className="text-xs text-slate-400">{r.features.join(' • ')}</span><button onClick={()=>toggle(r.id)} className={'rounded-lg px-3 py-2 text-xs font-bold '+(r.status==='blocked'?'bg-emerald-50 text-emerald-700':'bg-rose-50 text-rose-700')}>{r.status==='blocked'?'Desbloquear':'Bloquear'}</button></div></div>)}</div></>}
